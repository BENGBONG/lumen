# 2026-08-13

## 项目进度盘点（Lumen / 类forklift-项目）

- **已提交**（2026-05-08 "闪念初版完成"）：阶段1+2（双窗格/多标签/传输队列/主题/收藏/15 单测）。
- **未提交**（5月14–26日工作，~1300 行改动 + 新文件）：改名 Lumen、AIKit 包（724 行，untracked）、App/Views/AI/（671 行，untracked）、AI 设置页、AI 搜索、传输 Inspector 重构、压缩/解压、Cmd+C/X/V、F2 内联重命名；协作协议 AGENTS.md / docs/COLLAB.md / tasks/ 也全部 untracked。
- **验证**：当前工作区 `swift build` 通过（需 `--disable-sandbox`，会话环境拦 sandbox-exec；这本身是个可复用经验）。
- **风险**：项目寄生在 `/Users/admin/Desktop/AI code` 大 monorepo（远端是 baixiao-next-html，别的项目），当前分支 `codex/baizhi-v2-1-release`；Lumen 两个半月工作未提交，与几十个项目改动混在同一工作区。
- **任务队列**：tasks/ 从未启用，无 open/归档任务；README 停留在阶段2，已过时。
- **建议下一步**：P0 独立 git 仓库保住成果 → P1 同步 README → P2 用户指派角色启用 COLLAB 流水线（候选：阶段3 归档浏览/冲突弹窗/远程协议，或 AI 功能打磨）。

## 执行结果（用户决策：独立仓库 + 我自己规划执行，不走 COLLAB 角色制）

1. **P0 完成**：`git init -b main` 独立仓库，root commit `cfb598c`（56 文件 6891 行）。父级 monorepo 未动——它还跟踪着本目录旧文件，后续若要彻底清理需在父仓库侧 `git rm --cached`（已告知用户）。
2. **P1 完成**：README 重写为 Lumen 现状，commit `8882a19`。
3. **首个功能完成——传输冲突弹窗**，commit `b0472e6`：
   - 发现 TransferQueue 持有 resolver 但从未调用（冲突=任务直接失败）
   - FileProvider 加 `item(at:)`（默认 nil）；ConflictResolution 加 `.cancel`；TransferStatus 加 `.skipped`；TransferTask.destination 改 var
   - drain() 接通 resolver：覆盖=先删目标 / 重命名=自动编号去重（"副本 2"）/ .merge 显式抛 mergeUnsupported
   - App 层 UserConflictResolver（NSAlert：保留两者[默认]/覆盖/跳过/停止 + 应用到所有冲突）
   - 新增 7 个单测，22/22 全绿；App 编译通过；Lumen.app 构建并启动成功
4. **下一步候选**（按价值排序）：zip/tar 虚拟目录浏览、目录合并（.merge 已预留）、AI 功能端到端打磨、SFTP/S3/WebDAV（需引入依赖，L 级）。
5. **环境备忘**：本会话跑 swift build/test 必须加 `--disable-sandbox`（sandbox-exec 被系统拦）。
