# 2026-08-13

## 项目进度盘点（Lumen / 类forklift-项目）

- **已提交**（2026-05-08 "闪念初版完成"）：阶段1+2（双窗格/多标签/传输队列/主题/收藏/15 单测）。
- **未提交**（5月14–26日工作，~1300 行改动 + 新文件）：改名 Lumen、AIKit 包（724 行，untracked）、App/Views/AI/（671 行，untracked）、AI 设置页、AI 搜索、传输 Inspector 重构、压缩/解压、Cmd+C/X/V、F2 内联重命名；协作协议 AGENTS.md / docs/COLLAB.md / tasks/ 也全部 untracked。
- **验证**：当前工作区 `swift build` 通过（需 `--disable-sandbox`，会话环境拦 sandbox-exec；这本身是个可复用经验）。
- **风险**：项目寄生在 `/Users/admin/Desktop/AI code` 大 monorepo（远端是 baixiao-next-html，别的项目），当前分支 `codex/baizhi-v2-1-release`；Lumen 两个半月工作未提交，与几十个项目改动混在同一工作区。
- **任务队列**：tasks/ 从未启用，无 open/归档任务；README 停留在阶段2，已过时。
- **建议下一步**：P0 独立 git 仓库保住成果 → P1 同步 README → P2 用户指派角色启用 COLLAB 流水线（候选：阶段3 归档浏览/冲突弹窗/远程协议，或 AI 功能打磨）。
